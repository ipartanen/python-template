[project]
name = "{{project_name}}"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.42.0",
]

[dependency-groups]
dev = [
    "deptry>=0.25.1",
    "ruff>=0.14.5",
]

[tool.deptry.per_rule_ignores]
DEP001 = ["classes", "core", "services", "helpers", "controllers"]
